
/*
// let's use " rafce " to create a functional component and remember we don't need to
// include " ; " so I'll try it and see if I like it 
import React from 'react';

// now let's create the footer file and go to that file now

// 1 -
// coming from the app.styles.scss file
// import in the NavBar component
import NavBar from '../nav-bar/nav-bar.component';

import './header.styles.scss';

// End of 1 -

const Header = () => {

    return (

        <header>

            <NavBar />

        </header>

    );

}

export default Header;
*/
























// ===============================
























// at the beginning of lecture 45, given all the notes, I created a new file below without
// the notes so we are starting fresh

import React from 'react';
// import in the NavBar component
import NavBar from '../nav-bar/nav-bar.component';

import './header.styles.scss';


const Header = () => {

    return (

        <header>

            <NavBar />

        </header>

    );

}

export default Header;
